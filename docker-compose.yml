version: "3.8"
services:
  bot:
    container_name: nachomemes
    build:
      context: .
      target: development
      dockerfile: Dockerfile
    volumes:
      - "./:/app"
    environment:
      - DISCORD_TOKEN
      - ACCESS_KEY
      - SECRET
      - REGION
